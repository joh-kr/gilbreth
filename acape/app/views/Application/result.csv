%{ if(interviews != null) { }%
	#{list attributes, as:'attribute'}*{
		}*#{list attribute.getLevels(null), as:'level'}*{
			}*"${attribute.name}.${level.name}${level.getFeatures() == null ? '' : '.' + '{' + level.getFeatures() + '}'}"*{
			}*${attribute_isLast ? '' : ','}*{
		}*#{/list}*{
	}*#{/list}
	
	*{And now the interviews}*
	#{list interviews, as:'interview'}*{
	}*%{models.Utility utility = new models.Utility(interview.result);}%*{
		}*#{list attributes, as:'attribute'}*{
			}*#{list attribute.getLevels(null), as:'level'}*{
				}*%{utility.computeCalibratedUtilityFor(level).round(2);}%*{
				}*${attribute_isLast ? '' : ','}*{
			}*#{/list}*{
		}*#{/list}*{
	}*#{/list}	
%{ } else {empty} }% 
